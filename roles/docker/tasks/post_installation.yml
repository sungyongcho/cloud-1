- name: Create docker group
  group:
    name: docker
    state: present
  become: yes

- name: Add current user to docker group
  user:
    name: "{{ current_user.stdout }}"
    groups: docker
    append: yes
  become: yes

